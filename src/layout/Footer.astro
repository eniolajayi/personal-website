---
const date = new Date();
const currentYear = date.getFullYear();
---

<footer class="footer">
  <div class="footer__container">
    <a href="">proverbs 22:29</a>
    <small title="All rights reserved">&#169; {currentYear}</small>
    <a href="">ajayi</a>
  </div>
</footer>

<style lang="scss">
  .footer {
    background-color: var(--gray-11);

    &__container {
      inline-size: 100%;
      min-block-size: var(--size-12);
      padding-inline: var(--size-fluid-4);
      padding-block: var(--size-fluid-1);
      font-size: var(--font-size-base);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
  }
  small {
    text-transform: uppercase;
    font-weight: 800;
    font-stretch: 75%;
    color: var(--gray-0);
  }

  a {
    color: var(--gray-0);
  }
</style>
